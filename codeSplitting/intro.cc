#include "intro/calc.hpp"
#include <print>
#include <string>

// We will be discussing code organisation using
// 1: header files
// 2: namespaces

int main() {
  std::string name;
  int height;
  std::println("sum of 2 n 3 is: {}", sum(2, 3));
}
